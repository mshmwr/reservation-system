import React from "react";
import "./ShowMoreIcon.css";
const ShowMoreIcon = ({ clickHandler }) => {
  return (
    <div className="showMoreIcon" onClick={clickHandler}>
      ...
    </div>
  );
};
export default ShowMoreIcon;
